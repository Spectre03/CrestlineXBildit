/**
 * BILDIT bundled admin resolves `react` and `react-dom/client` from `window`.
 * Both must use the same instances as the Hydrogen app.
 */
export declare function ensureHostReactGlobals(): void;
